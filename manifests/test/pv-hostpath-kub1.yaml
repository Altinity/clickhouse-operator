apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-disk
  labels:
    type: pv-disk
    where: kub1
spec:
  storageClassName: manual
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/pv-disk"

