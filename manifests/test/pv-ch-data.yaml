apiVersion: v1
kind: PersistentVolume
metadata:
  name: clickhouse-data
  labels:
    type: local
spec:
#  storageClassName: manual
  capacity:
    storage: 300Mi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/clickhouse-data"

