apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-local-noclass-kub2
  labels:
    name: pv-local-noclass-kub2
spec:
  capacity:
    storage: 2Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
#  storageClassName: local-storage
  local:
    path: /pv-local
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - kubnode1

