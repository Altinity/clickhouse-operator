apiVersion: clickhouse.altinity.com/v1
kind: ClickHouseInstallation
metadata:
  name: test-017-multi-version
spec:
  templates:
    podTemplates:
    - name: v20.3
      spec:
        containers:
        - name: clickhouse-pod
          image: yandex/clickhouse-server:20.8.6.6
    - name: v20.8
      spec:
        containers:
        - name: clickhouse-pod
          image: altinity/clickhouse-server:21.8.altinity_prestable
  configuration:
    clusters:
    - name: default
      layout:
        shards:
         - templates:
             podTemplate: v20.3
         - templates:
             podTemplate: v20.8
    files:
      users.d/remove_database_ordinary.xml: |
        <yandex>
          <profiles>
             <default>
                <default_database_engine remove="1"/>
             </default>
          </profiles>
        </yandex>
