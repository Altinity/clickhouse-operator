apiVersion: "clickhouse.altinity.com/v1"
kind: "ClickHouseOperatorConfiguration"
metadata:
  name: "test-063-remote-servers-max-fragments"
spec:
  clickhouse:
    configuration:
      remoteServers:
        remoteServersSplitThresholdBytes: 500
        maxRemoteServersFragments: 1
