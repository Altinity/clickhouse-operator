apiVersion: "clickhouse.altinity.com/v1"
kind: "ClickHouseOperatorConfiguration"
metadata:
  name: "test-033-chopconf"
spec:
  clickhouse:
    access:
      # Port where to connect to ClickHouse instances to
      schema: https
      port: 8443
